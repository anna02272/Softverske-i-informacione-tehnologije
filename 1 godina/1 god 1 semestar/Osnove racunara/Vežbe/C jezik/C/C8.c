#include <stdio.h>


void main()
{
    int h, min:
        long int sek;

    printf("\nUpisi broj sati i minuta: ");
        sanf("%d%d", &h, &min);

        sek = (h*60+min)*60;

        printf("\n%d sati i %d minuta iznosi %ld sekunid, h, min, sek");

    return;
}
